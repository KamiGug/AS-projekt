class BaseScene extends Phaser.Scene {
    preload () {
        // super.preload()
    }

    load () {
        super.load()
    }

    create () {
        super.create()
    }

    update (time, delta) {
        super.update(time, delta)
    }
}
